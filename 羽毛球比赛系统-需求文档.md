# 校园羽毛球比赛管理系统 — 产品需求文档

> 本文档既是产品需求规格(PRD),也可直接作为 AI 编程工具(Cursor / Claude Code / v0 等)的提示词使用。

---

## 一、项目概述

构建一个**校园羽毛球比赛管理系统**(Web 应用),面向校园社团使用。覆盖赛事配置、选手报名、抽签编排、场地分配、裁判实时记分、数据大屏、历届数据等全流程。

### 角色

| 角色 | 登录方式 | 权限范围 |
|---|---|---|
| **总管理员** | 账号密码 | 所有权限:建赛事、录入选手、配置规则、抽签、排程、创建裁判账号、查看一切 |
| **裁判** | 账号密码(管理员创建分配) | 仅能给被指派的场次记分 |
| **观众** | 无需登录 | 公开访问首页大屏、赛程、排名、历届数据 |

---

## 二、技术栈

| 层 | 选型 |
|---|---|
| 前端 | **Next.js**(App Router) + **TypeScript** + **Ant Design** |
| 后端 | **NestJS** + **TypeScript** |
| 数据库 | **PostgreSQL** |
| ORM | **Prisma** |
| 实时通信 | **Socket.IO**(NestJS 内置 WebSocket Gateway) |
| 认证 | NextAuth.js(前端会话) + Passport-JWT(后端鉴权) |
| 表单 / 校验 | React Hook Form + Zod |
| 文件导出 | ExcelJS(Excel) |
| 部署 | 自有服务器 + Nginx 反向代理 + PM2 / Docker |

---

## 三、功能模块详述

### 模块 1:赛事 & 单项

- 一届赛事下可同时设置多个**单项**:男单、女单、男双、女双、混双
- 每个单项**独立配置**赛制、计分规则,独立运行赛程
- 单项可独立举办,也可作为团体赛的子项
- 系统**保留历届赛事数据**,可查阅往届名次、对阵、选手信息

### 模块 2:选手管理

- 选手由**总管理员后台统一录入**(支持单条录入和 Excel 模板批量导入)
- 选手基本信息:姓名、性别、学院/班级、联系方式、备注
- **报名限制**:同一选手最多可报 **2 个单项**
  - 该限制可在后台开关配置(允许 / 不允许跨项目)
  - 关闭跨项目时,提交报名超额会**弹出后台提示并阻止保存**
- **双打报名**:在双打项目内直接输入两位搭档姓名,系统自动同步记录到选手库

### 模块 3:团体赛

#### 队伍

- 团体赛与单项赛**完全独立**,是两套赛事体系
- 队伍为独立实体(队名、所属单位、队员列表)
- 每队**至少 10 人**
- 队员录入方式三选一:
  1. **从选手库勾选**已录入的选手
  2. **Excel 模板批量导入**到选手库后勾选
  3. **快速提示词录入**:输入如 `单打 张三,双打 李四 王五,混双 赵小 孙七` 这种格式,系统自动解析并配对

#### 团体赛结构

- 固定由 5 个单项组成,出场顺序:**男单 → 女单 → 男双 → 女双 → 混双**(顺序后台可调整)
- 后台可配置**胜场数**:
  - 3 项 2 胜制(如:男单 + 女单 + 混双)
  - 5 项 3 胜制(全部 5 项)
- 一方先达到胜场数,系统**自动判定团体赛结束**,剩余项目不打
- 若 1-1 / 2-2 平,继续打下一项决胜
- 队员**最多身兼 2 项**

#### 出场名单

- 比赛开始**前**,由管理员在该团体赛中设置每个项目的出场选手
- 设置好后锁定,比赛进行中不可更改

### 模块 4:赛制 & 抽签

#### 赛制(每个单项独立配置)

- **单淘汰**
- **小组循环 + 淘汰**
  - 小组人数、每组出线名额后台可配
  - 出线后**交叉对阵**:A 组第 1 vs B 组第 2,B 组第 1 vs A 组第 2,以此类推

#### 种子选手

- 后台可指定种子选手
- **自动模式**:系统按种子规则分到不同区/不同组,避免提前相遇
- **手动模式**:管理员可手动拖拽调整种子位置

#### 抽签

- 默认系统自动抽签
- 抽签结果支持管理员**手动调整**(拖拽 / 替换)
- 抽签结果生成后冻结,需"重新抽签"按钮才能再次操作

### 模块 5:计分规则

每个单项后台可选以下任一规则:

- 15 分 1 局
- 15 分 3 局 2 胜
- 21 分 3 局 2 胜
- 31 分 3 局 2 胜

对每种规则,可单独设置:

- **封顶 30 分模式**:20 平(或对应平分)后需领先 2 分,封顶 30 分(到 30 分先到者胜)
- **标准金球制**:谁先到规定分数谁胜,无平分加赛

### 模块 6:实时记分

#### 裁判端(响应式,**必须适配手机/平板**)

- 裁判账号由总管理员后台创建并分配
- 一个裁判可负责**多场**比赛
- 裁判登录后可看到自己被指派的全部场次列表

#### 记分功能

- 实时记分:每得一分点击一次
- **撤销上一分**(必须有,防止误触)
- 自动判定局胜负与场胜负(按所选计分规则)
- 比分变化通过 **WebSocket 实时推送**到大屏与观众端

#### 比赛过程记录

- 发球方切换记录
- 暂停(普通暂停)
- 医疗暂停
- 警告
- 黄牌

### 模块 7:场地与排程

- 后台维护场地列表(1 号场 / 2 号场 / …)
- **系统自动排程**:根据赛程、场地数、预估时长自动安排
- 自动排程后,管理员**手动校错**(拖拽调整)
- **冲突检测**:同一选手不能同时被排两场;同一场地同一时段不能有两场比赛,冲突时高亮提示

### 模块 8:数据大屏(网站首页)

- 公开访问,无需登录
- **进入首页即为大屏视图**,固定布局
- 内容板块:
  - 正在进行的比赛(各场地实时比分,WebSocket 实时刷新)
  - 即将开始的比赛(场地、时间、对阵)
  - 各单项最新战报(刚结束的场次)
  - 当前赛事名称、届次
- **响应式**:电脑端为主,手机能正常浏览即可

### 模块 9:其他公开页面

- **完整赛程页**:展示所有单项 + 团体赛的完整赛程与对阵图(bracket)
- **排名页**:各单项实时排名、团体赛排名
- **选手页**:选手列表与个人页(参赛项目、对阵记录)
- **历届数据页**:可翻看往届赛事的名次、对阵、选手

### 模块 10:数据导出

支持导出 **Excel** 格式:

- **赛程表**:按单项 / 团体赛 / 场地 / 时间维度导出
- **成绩册**:含每场比分、胜负、名次
- **报名表**:含选手信息、报名项目

---

## 四、数据模型(Prisma Schema 草图)

```prisma
// 赛事(届次)
model Tournament {
  id          String   @id @default(cuid())
  name        String
  edition     Int      // 第几届
  startDate   DateTime
  endDate     DateTime
  isArchived  Boolean  @default(false)
  events      Event[]
  teamMatches TeamMatch[]
  venues      Venue[]
  createdAt   DateTime @default(now())
}

// 单项
model Event {
  id            String   @id @default(cuid())
  tournamentId  String
  tournament    Tournament @relation(fields: [tournamentId], references: [id])
  type          EventType // MENS_SINGLES, WOMENS_SINGLES, MENS_DOUBLES, WOMENS_DOUBLES, MIXED_DOUBLES
  format        Format    // SINGLE_ELIMINATION, GROUP_PLUS_KNOCKOUT
  scoringRule   ScoringRule // FIFTEEN_ONE, FIFTEEN_BO3, TWENTYONE_BO3, THIRTYONE_BO3
  scoringMode   ScoringMode // CAPPED_30, STANDARD_GOLDEN
  registrations Registration[]
  matches       Match[]
}

// 选手
model Player {
  id            String @id @default(cuid())
  name          String
  gender        Gender
  affiliation   String  // 学院/班级
  contact       String?
  notes         String?
  registrations Registration[]
}

// 报名(支持单打/双打)
model Registration {
  id        String  @id @default(cuid())
  eventId   String
  event     Event   @relation(fields: [eventId], references: [id])
  player1Id String
  player1   Player  @relation("p1", fields: [player1Id], references: [id])
  player2Id String? // 双打才有
  player2   Player? @relation("p2", fields: [player2Id], references: [id])
  isSeed    Boolean @default(false)
  seedRank  Int?
  groupName String? // 小组名
}

// 比赛
model Match {
  id          String  @id @default(cuid())
  eventId     String?
  event       Event?  @relation(fields: [eventId], references: [id])
  teamMatchId String? // 团体赛的子场次
  teamMatch   TeamMatch? @relation(fields: [teamMatchId], references: [id])
  round       String  // GROUP, R16, QF, SF, F 等
  side1Id     String  // Registration ID 或 Team 出场 ID
  side2Id     String
  refereeId   String?
  referee     User?   @relation(fields: [refereeId], references: [id])
  venueId     String?
  venue       Venue?  @relation(fields: [venueId], references: [id])
  scheduledAt DateTime?
  status      MatchStatus // PENDING, LIVE, COMPLETED
  games       Game[]
  events      MatchEvent[] // 暂停、医疗暂停、警告、黄牌
  winnerSide  Int?
}

// 局
model Game {
  id        String @id @default(cuid())
  matchId   String
  match     Match  @relation(fields: [matchId], references: [id])
  gameNo    Int
  side1Score Int @default(0)
  side2Score Int @default(0)
  server    Int?  // 1 或 2
  winnerSide Int?
}

// 比赛过程事件
model MatchEvent {
  id        String @id @default(cuid())
  matchId   String
  match     Match  @relation(fields: [matchId], references: [id])
  type      EventLogType // TIMEOUT, MEDICAL_TIMEOUT, WARNING, YELLOW_CARD, SERVE_CHANGE
  side      Int?
  note      String?
  createdAt DateTime @default(now())
}

// 团体赛
model TeamMatch {
  id           String  @id @default(cuid())
  tournamentId String
  tournament   Tournament @relation(fields: [tournamentId], references: [id])
  team1Id      String
  team1        Team    @relation("team1", fields: [team1Id], references: [id])
  team2Id      String
  team2        Team    @relation("team2", fields: [team2Id], references: [id])
  format       TeamFormat // BEST_OF_3, BEST_OF_5
  order        Json    // 子项目出场顺序
  lineup1      Json    // 队 1 各项目出场名单
  lineup2      Json
  matches      Match[]
  winnerTeamId String?
  status       MatchStatus
}

// 队伍
model Team {
  id      String   @id @default(cuid())
  name    String
  affiliation String
  members Player[] @relation("TeamMembers")
}

// 场地
model Venue {
  id           String @id @default(cuid())
  tournamentId String
  tournament   Tournament @relation(fields: [tournamentId], references: [id])
  name         String  // 1 号场地
  matches      Match[]
}

// 用户(管理员、裁判)
model User {
  id       String @id @default(cuid())
  username String @unique
  password String // bcrypt
  role     Role   // ADMIN, REFEREE
  matches  Match[]
}
```

---

## 五、关键交互流程

### 流程 1:开赛前准备(管理员)

1. 创建一届赛事(填基本信息)
2. 录入选手(单条 / Excel 批量)
3. 创建本届的单项(选类型、配赛制、配计分规则)
4. 给单项添加报名(选手 / 搭档)
5. 标记种子选手
6. 创建队伍(团体赛用),配置队员
7. 创建团体赛对阵
8. 设置场地列表
9. 创建裁判账号

### 流程 2:抽签 & 排程

1. 各单项点击「抽签」,系统按种子规则生成对阵图
2. 管理员检查、必要时手动微调
3. 点击「自动排程」,系统按场地数 + 预估时长生成时间表
4. 管理员手动校错冲突
5. 给每场比赛指派裁判
6. 一键发布,公开页和大屏同步显示

### 流程 3:比赛进行中(裁判)

1. 裁判手机登录,看到自己负责的场次
2. 选择当前场次,进入记分页
3. 实时点击「side1 +1 / side2 +1」记分
4. 误点可「撤销」
5. 需要时记录暂停 / 黄牌等事件
6. 局结束自动判胜,自动开启下一局
7. 场结束自动判胜方,场地释放,刷新到大屏与赛程页

### 流程 4:赛后归档

1. 全部赛程结束后,管理员「归档本届赛事」
2. 数据进入历届数据页,只读
3. 管理员可导出 Excel(赛程表 / 成绩册 / 报名表)

---

## 六、UI / 响应式要求

| 端 | 适配要求 |
|---|---|
| 管理员后台 | 电脑端优先,使用 Ant Design Pro 风格 |
| 裁判记分页 | **必须响应式**,手机和平板优先,大按钮、清晰比分显示 |
| 数据大屏(首页) | 电脑端 / 投屏优先,固定布局,手机能浏览即可 |
| 公开页(赛程、排名、选手、历届) | 电脑、手机都要可看 |

---

## 七、不在本期范围内

明确**不做**的功能(避免开发偏题):

- ❌ 公告发布
- ❌ 电子证书 / 颁奖名单生成
- ❌ 选手历史统计(冠军榜、生涯胜率)
- ❌ 报名缴费
- ❌ 选手账号
- ❌ 微信扫码 / 手机验证码登录
- ❌ 移动端 App(纯 Web,响应式即可)

---

## 八、部署架构建议

```
[用户浏览器]
     │
     ▼
[Nginx]  ── 80/443
   ├──► [Next.js (前端) :3000]  ── pm2
   └──► [NestJS (后端) :4000]   ── pm2
                │
                ├──► [PostgreSQL :5432]
                └──► [Socket.IO (实时记分)]
```

- 前后端各跑一个 Node 进程,用 PM2 守护
- 也可用 Docker Compose 一键起 4 个容器(Next、Nest、PG、Nginx)
- HTTPS 用 Let's Encrypt 免费证书
- 数据库定期备份(pg_dump 定时任务)

---

## 九、给 AI 编程工具的执行建议

如果你把本文档喂给 ai让它来写代码,建议**分阶段**:

1. **第一阶段**:搭骨架——初始化 Next.js + NestJS + Prisma 项目,跑通登录、选手 CRUD、单项 CRUD
2. **第二阶段**:抽签算法 + 对阵图渲染
3. **第三阶段**:裁判记分页 + WebSocket 实时同步
4. **第四阶段**:场地自动排程 + 冲突检测
5. **第五阶段**:团体赛模块(数据模型最复杂,放后面)
6. **第六阶段**:大屏、历届数据、Excel 导出

每个阶段独立可测试,不要一次性让 AI 把所有东西写完。

---

**文档结束。建议保存到项目仓库的 `docs/PRD.md`,后续需求变更直接在此基础上 diff。**
